# Dev environment variables — DO NOT commit real values
# Generate secrets with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VAULT_PASSWORD=
JWT_SECRET=
ALLOWED_ORIGINS=https://tanquecheio.pages.dev,http://localhost:5173,http://localhost:5174
